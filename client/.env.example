# Client Environment Variables
# In production, set these in the Vercel Dashboard

# API base URL — leave empty for same-domain Vercel deployment
# Set this only if your API is on a different domain
VITE_API_URL=
